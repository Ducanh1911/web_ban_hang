@model IEnumerable<BaiTap.Models.Category>

@{
    Layout = "~/Areas/Admin/Views/Shared/Admin_Layout.cshtml";

}


<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên danh mục
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.categoryName)
        </td>
       
    </tr>
}

</table>
